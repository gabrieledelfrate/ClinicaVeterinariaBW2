@model IEnumerable<ClinicaVeterinaria.Models.Product>

@if (!Model.Any())
{
    <div>Nessuna medicina trovata per questa data.</div>
}
else
{
    <ul>
        @foreach (var item in Model)
        {
            <li>@item.Nome</li>
        }
    </ul>
}
